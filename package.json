{
  "name": "binaural-beats",
  "version": "1.0.0",
  "private": true,
  "description": "Offline stereo binaural tone generator",
  "scripts": { "test": "node --test tests/*.test.cjs" },
  "devDependencies": { "playwright": "^1.58.2" }
}
